<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
        <link rel="stylesheet" href="/css/styles.css" type="text/css">
    </head>
    <body>
        <div class = "container">
            <div class="jumbotron">
                <h1>Scissorhands Salon</h1>
            </div>
            <div class="form-div">
                <form class="form-group" action="/add_stylist" method="post">
                    <label for="name">Add Stylist name:</label>
                    <input type="text" name="name" placeholder="name"><br>
                    <label for="description">Add Stylist bio:</label>
                    <input type="text" name="description" placeholder="bio"><br>
                    <button class="btn">Add Stylist</button>
                </form>
                {% if stylists is not empty %}
                <h3>Available Stylists:</h3>
                <ul>
                    {% for stylist in stylists %}
                        <li><a href="/stylists/{{stylist.getId()}}">{{stylist.getName()}}</a></li>
                    {% endfor %}
                </ul>
                {% endif %}
            </div>
        </div>
    </body>
</html>
